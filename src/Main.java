public class Main {
    public static void main(String[] args) {

        /*Library library = Library.getInstance();
        Librarian librarian = new Librarian("12234");
        Book book = new Book(1, "Mr X", "X for Test", 10.5, BookStatus.AVAILABLE, "1st", LocalDateTime.now());
        List<Book> books = new ArrayList<>();
        books.add(book);
        Reader reader = new Reader(books);
        MemberRecord record = new MemberRecord(reader);

        Book book2 = new Book(2, "Mr T", "T for Test", 12.5, BookStatus.AVAILABLE, "1st", LocalDateTime.now());
        library.newBook(book2);
        reader.requestBorrow(book2,librarian);*/

    }
}